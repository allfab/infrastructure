COMPOSE_PROJECT_NAME=webapps

# PORTAINER
AGENT_SECRET="{{ portainer.agent.secret.key }}"

# HOMEPAGE
HOMEPAGE_VAR_PROXMOX_USER="{{ homepage.proxmox.pam }}"
HOMEPAGE_VAR_PROXMOX_PASSWORD="{{ homepage.proxmox.token }}"
HOMEPAGE_VAR_PROXMOX_BACKUP_USER="{{ homepage.pbs.pam }}"
HOMEPAGE_VAR_PROXMOX_BACKUP_PASSWORD="{{ homepage.pbs.token }}"
HOMEPAGE_VAR_PORTAINER_TOKEN="{{ homepage.portainer.token }}"
HOMEPAGE_VAR_OPENWEATHERMAP_APIKEY="{{ homepage.openweathermap.apikey }}"
HOMEPAGE_VAR_PIHOLE_TOKEN="{{ homepage.pihole.apikey }}"
HOMEPAGE_VAR_UNIFI_USER="{{ homepage.unifi.user }}"
HOMEPAGE_VAR_UNIFI_PASSWORD="{{ homepage.unifi.password }}"

# LINKACE
APP_KEY="base64:1iMbqVUXq2YTrUSk6cda7BXS3WKLY9SgEG7+TTEwfrA=" # openssl rand -base64 32
DB_CONNECTION="pgsql"
DB_HOST="192.168.10.20"
DB_PORT="5432"
DB_DATABASE="linkace"
DB_USERNAME="myuser"
DB_PASSWORD="mypassword"
SETUP_COMPLETED=true
#INITDB
# docker exec -it linkace php artisan key:generate
# docker exec -it linkace php artisan migrate
# docker exec -it linkace php artisan setup:complete
# docker exec -it linkace php artisan registeruser