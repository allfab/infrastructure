COMPOSE_PROJECT_NAME=monitoring

# PORTAINER
AGENT_SECRET="{{ portainer.agent.secret.key }}"

# HEALTHCHECKS
HEALTHCHECKS_SECRET_KEY="{{ healthchecks.secret.key }}" # openssl rand -base64 64
HEALTHCHECKS_SUPERUSER_EMAIL="{{ secret.users.me.name }}@gmail.com"
HEALTHCHECKS_SUPERUSER_PASSWORD="{{ secret.users.me.password }}"
